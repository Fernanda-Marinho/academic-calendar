{
  "name": "academic-calendar",
  "version": "1.0.0",
  "private": true,
  "description": "Parser para calendário acadêmico (PDF) da UEFS, convertendo em eventos para o Google Agenda via arquivos .ics",
  "scripts": {
    "dev": "concurrently \"node server/index.js\" \"(cd client && npm run dev)\"",
    "build": "cd client && npm run build",
    "start": "node server/index.js",
    "server": "node server/index.js",
    "client": "cd client && npm run dev"
  },
  "dependencies": {
    "pdf-parse": "^1.1.1",
    "cheerio": "^1.0.0",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "concurrently": "^8.2.0"
  }
}
