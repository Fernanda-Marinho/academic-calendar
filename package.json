{
  "name": "academic-calendar",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.0.0",
  "description": "Parser para calendário acadêmico (PDF) da UEFS, convertendo em eventos para o Google Agenda via arquivos .ics",
  "scripts": {
    "dev": "concurrently \"node server/index.js\" \"(cd client && npm run dev)\"",
    "build": "cd client && npm install && npm run build",
    "update-calendars": "node scripts/update-calendars.js",
    "start": "node server/index.js",
    "server": "node server/index.js",
    "client": "cd client && npm run dev"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "concurrently": "^8.2.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0",
    "pdf-parse": "^1.1.1"
  }
}
